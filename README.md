HireSphere — Job Application Platform

## File Uploads
- Resumes are stored locally in the `uploads/` directory
- The directory is excluded from Git using `.gitignore`
- Files are served via Spring ResourceHandler
